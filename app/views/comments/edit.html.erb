<h1>Editing comment</h1>

<%= form_for([:post, @comment]) do |f| %>
  <div class="field">
    <%= f.label :commentor %> <br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <p><%= f.submit %></p>
<% end %>
